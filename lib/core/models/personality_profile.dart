/// PersonalityProfile compatibility export
///
/// `PersonalityProfile` is defined in `package:spots_ai`, but parts of the app
/// (and sibling packages like `spots_knot`) historically imported it from:
/// `package:spots/core/models/personality_profile.dart`.
///
/// This file preserves that stable import path.
library;
export 'package:spots_ai/models/personality_profile.dart';

